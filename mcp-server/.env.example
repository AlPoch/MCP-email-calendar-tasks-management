# Server Configuration
PORT=3000

# Folder for unimportant emails (processed by AI)
EMAIL_UNEMPOTANT_ORDER=GPTAussortiert

# --- EMAIL ACCOUNTS ---
# Add any number of accounts by incrementing the index: EMAIL_1_..., EMAIL_2_..., etc.
# The loader will continue until it finds no EMAIL_n_USER.

# Attributes:
# - NAME: Display name for the account.
# - USER: Email address / username.
# - PASSWORD: Password or App Password.
# - IMAP_HOST / SMTP_HOST: Provider server addresses.
# - IMAP_PORT: Defaults to 993.
# - SMTP_PORT: Defaults to 587.
# - SMTP_SECURE: 
#   - 'true' for Port 465 (Direct SSL - e.g., Gmail).
#   - 'false' for Port 587 (STARTTLS - e.g., GMX, Outlook).
#   - Default: true for 465, false otherwise.
EMAIL_1_NAME=GMX_Haupt
EMAIL_1_USER=your-email@gmx.de
EMAIL_1_PASSWORD=your-password
EMAIL_1_IMAP_HOST=imap.gmx.net
EMAIL_1_IMAP_PORT=993
EMAIL_1_SMTP_HOST=mail.gmx.net
EMAIL_1_SMTP_PORT=587
EMAIL_1_SMTP_SECURE=false

# EMAIL 2: Gmail Example
# IMPORTANT: For Google, use an "App Password" if 2FA is enabled.
EMAIL_2_NAME=Google_Work
EMAIL_2_USER=your-email@gmail.com
EMAIL_2_PASSWORD=your-app-password
EMAIL_2_IMAP_HOST=imap.gmail.com
EMAIL_2_IMAP_PORT=993
EMAIL_2_SMTP_HOST=smtp.gmail.com
EMAIL_2_SMTP_PORT=465
EMAIL_2_SMTP_SECURE=true

# Google Calendar/Tasks Configuration (OAuth2)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# MCP Server Auth
MCP_CLIENT_ID=mcp-client
MCP_CLIENT_SECRET=mcp-secret
MCP_STATIC_TOKEN=your-static-token

# Ngrok (Optional)
NGROK_AUTHTOKEN=your-ngrok-token
